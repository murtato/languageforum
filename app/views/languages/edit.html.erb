<h1>Misspelled a language?</h1>

<%= form_for @language do |f| %>

    <div>
        <%= f.label :name %>
        <%= f.text_field :name %>
    </div>

    <%= f.submit "Submit these changes!" %>

<% end %>

<%= link_to "Back to the Cafe!", languages_path %>
